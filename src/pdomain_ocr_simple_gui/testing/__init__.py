"""Testing helpers for pdomain-ocr-simple-gui.

This sub-package contains test-only utilities (fake dispatchers, fixtures,
seam helpers) that let integration tests and e2e tests run without model
weights.  It must not be imported by production code.
"""
